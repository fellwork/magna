$schema: 'https://moonrepo.dev/schemas/project.json'

type: 'library'
language: 'rust'

dependsOn:
  - 'magna-types'

tasks:
  build:
    command: 'cargo build -p magna-config'
    inputs: ['src/**/*.rs', 'Cargo.toml']
    deps:
      - 'magna-types:build'

  test:
    command: 'cargo test -p magna-config'
    inputs: ['src/**/*.rs', 'Cargo.toml']
    deps:
      - '~:build'
