$schema: 'https://moonrepo.dev/schemas/project.json'

type: 'library'
language: 'rust'

tasks:
  build:
    command: 'cargo build -p fw-graph-dataplan'
    inputs: ['src/**/*.rs', 'Cargo.toml']

  test:
    command: 'cargo test -p fw-graph-dataplan'
    inputs: ['src/**/*.rs', 'Cargo.toml']
    deps:
      - '~:build'
